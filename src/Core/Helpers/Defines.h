#pragma once

#include <glad/glad.h>

using uint = unsigned int;

using gl_string = std::basic_string<GLchar, std::char_traits<GLchar>, std::allocator<GLchar>>;

#define rcast reinterpret_cast
#define scast static_cast

